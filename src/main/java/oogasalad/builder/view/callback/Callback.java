package oogasalad.builder.view.callback;

// Commit: 945dc869 (callbacks were introduced as a way to refactor the rest of the view)
// Simply extended by other classes to define callbacks
// The type parameter being the return type helps make this system work
/**
 * A class defining a callback and containing the data needed to call that callback
 * @param <R> the return type of the callback
 */
public interface Callback<R> {

}
